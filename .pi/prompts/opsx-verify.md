---
description: Verify a change by running repo verification (unit tests)
---
Verify the repo by running:

```bash
pnpm test
```

If it fails, report:
- which command failed
- the error output summary
- what to do next
